__pycache__/
*.pyc
*:Zone.Identifier

# Pipeline working data (raw parquet weather captures, raw satellite PNGs) --
# never published, regeneratable by re-running the pipeline scripts. Keeping
# it out of git keeps this repo focused on the deployable site + source, not
# a ~100MB cache of source imagery.
pipeline/data/
